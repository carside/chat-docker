services:

  api:
    build:
      context: .
      dockerfile: prompt/Dockerfile
    ports:
      - 8000:8000

  ui:
    build:
      context: ./chat-ui
      dockerfile: Dockerfile
    ports:
      - 80:80
    depends_on:
      - api
